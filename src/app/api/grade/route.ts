import { NextResponse } from "next/server";

// Placeholder grading endpoint — real Gemini grading logic comes in a later step.
export async function GET() {
  return NextResponse.json({ status: "ok" });
}
